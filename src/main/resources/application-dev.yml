server:
  port: 8091
  hostname: localhost
  servlet:
    session:
      persistent: false

file:
  upload:
    kyc: C:/Workspace/final industrialproject/NG_DSA360/src/assets/images/kyc-docs
    customer: C:/Workspace/final industrialproject/NG_DSA360/src/assets/images/customer

logging:
  file:
    name: log/egram.log
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/master_egram
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
    hikari:
      auto-commit: true
      minimum-idle: 10
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      pool-name: HikariPool-DSA360
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    format-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        multiTenancy: DATABASE
        multi_tenant_connection_provider: multiTenantConnectionProvider
        tenant_identifier_resolver: currentTenantIdentifierResolver
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
  mail:
    host: smtp.gmail.com
    password: tgeq rdlf ekap zncn
    username: dsa360api@gmail.com
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
    metrics:
      enabled: true